// Run the private enterprise Skill UI test through the public host's Vitest
// environment so it resolves the same React, shared components, and aliases as
// a production enterprise renderer build.
import '@enterprise-modules/components/EnterpriseSkillTab.test'
