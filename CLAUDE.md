# Abu (阿布) — Claude Code 入口

@AGENTS.md

> 上面一行 `@AGENTS.md` 把本仓库规范的**唯一事实源** `AGENTS.md` 整篇导入（Claude Code 的文件导入语法）。
> **本仓库所有开发规范都写在 `AGENTS.md` 里**（Claude Code 与 Codex 共用），改规范只改那一份。
> 本文件只放 Claude Code 独有、Codex 没有的增量——目前没有仓库级独有项；跨端的模型路由分档见父目录 `../AGENTS.md`。
