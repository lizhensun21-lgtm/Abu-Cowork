spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:127.0.0.1}:${DB_PORT:5432}/${DB_NAME:abu_pm_dev}
    username: ${DB_USER:abu_pm_dev}
    password: ${DB_PASSWORD:abu_pm_dev_only}

logging:
  level:
    com.abu.server: DEBUG
    org.mybatis: DEBUG
